pipeline {

    agent any
    
    stages {
        stage("Step 1") {
            steps {
                echo "Hola mundo!!!"
                sh "hostname"
                sh "ip add"
            }
        }
    }
}
